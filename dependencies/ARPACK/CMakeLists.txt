#PROJECT(arpack C)
FILE( GLOB c_src f2c/*.c )
ADD_LIBRARY( arpack ${c_src} )
IF(NOT MSVC)
	INSTALL(TARGETS arpack ARCHIVE DESTINATION lib)
ELSE(NOT MSVC)
	INSTALL(TARGETS arpack ARCHIVE DESTINATION ${PROJECT_SOURCE_DIR}/bin)
ENDIF(NOT MSVC)
