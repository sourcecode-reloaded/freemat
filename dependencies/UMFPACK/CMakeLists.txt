CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
INCLUDE_DIRECTORIES( Include ../UFconfig ../AMD/Include Source)
INCLUDE( make_rules.cmake )
INSTALL( TARGETS umfpack ARCHIVE DESTINATION lib )
FILE(GLOB INST_H "Include/*.h")
INSTALL(FILES ${INST_H} DESTINATION include)
