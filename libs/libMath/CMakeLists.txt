ADD_DEFINITIONS( ${QT_DEFINITIONS} ) 
INCLUDE_DIRECTORIES( ${QT_INCLUDE_DIR} ${QT_QTCORE_INCLUDE_DIR}  )

IF( USE_DYNLINK )
	ADD_LIBRARY( blas
	blas_wrap.cpp blas_dyn_link.cpp
	)
	GET_TARGET_PROPERTY(BLAS_LIBRARY blas LOCATION)
ENDIF( USE_DYNLINK )
ADD_SUBDIRECTORY( libBLAS_C ) 


